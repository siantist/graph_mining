# from On the maximal solution of a linear system over tropical semirings
# pos determinant

# input: set of permutations s
def pos_determinant(s):
    tot_sum = 0
    for p in s:
        # take sum

        sigma= p
        prod = 1
        # take product
        for i in range(n)
            prod = prod*A[i, sigma(i)]

        tot_sum = tot_sum + prod

    return tot_sum
